# Frontend (Vite) — copy to .env.local for local dev, set in Vercel for prod
VITE_API_URL=http://localhost:8000

# Backend (FastAPI) — copy to backend/.env for local dev, set in Render for prod
GEMINI_API_KEY=your-google-gemini-api-key
